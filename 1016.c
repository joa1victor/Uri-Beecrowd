#include <stdio.h>

int main(){


        int distancia, tempo, minutos=2, km=1;

    scanf("%d", &distancia);
    tempo = (minutos*km)*distancia;
        printf("%d minutos\n", tempo);

    return 0;
}